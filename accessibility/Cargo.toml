[package]
name = "iced_accessibility"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
# TODO Ashley re-export more platform adapters

[dependencies]
accesskit = { version = "0.20.0" }
accesskit_unix = { version = "0.16.0", optional = true }
accesskit_windows = { version = "0.28.0", optional = true }
accesskit_macos = { version = "0.21.0", optional = true }
